package exigentech.algorithms;

import java.util.Arrays;
import org.junit.jupiter.api.Test;

class MergeSortTest {

  @Test
  void sort() {
    final Integer[] values = new Integer[]{5, 2, 99, 32};
//    Arrays.stream(new MergeSort<Integer>().sort(values)).forEach(v -> System.out.println(v));
  }
}